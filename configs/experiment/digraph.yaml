# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /data: digraph.yaml
  - override /net: multi_channel.yaml
  - override /trainer: default.yaml